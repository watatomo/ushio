import type { ReactNode } from "react";

function StoryController({ series, children }: { children: ReactNode }) {
    
    const 
    return <div>{children}</div>;
}

export default StoryController;
